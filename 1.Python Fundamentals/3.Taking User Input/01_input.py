a = input("Enter a number: ")
print(a)

b = input("Enter your name: ")
print(b)

c = input()
print(c)
print(type(c))
print(type(int(c)))

# always the user input is :
# an optional parameter that is used to display a message to the user before taking input. It can be used to provide instructions or context for the input being requested.
# taken as string, so we need to typecast it to integer if we want to perform any mathematical operation on it.