def square(x):
    return x*x  # here if we do x ** x, it would calculate x to the power of x, for example 2 ** 2 = 4, but 2 ** 3 = 8, so it would not give the square of the number, it would give the power of the number.

print(square(3))
print(square(5))
print(square(2))