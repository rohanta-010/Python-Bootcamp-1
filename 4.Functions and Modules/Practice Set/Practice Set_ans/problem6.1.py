def increment():
    counter = 0 # every time the function is called, counter is initialized to 0
    counter +=1 
    print(counter)

increment() # evry time the function is called, counter is initialized to 0 and then incremented by 1, so it will always print 1
increment()
increment()
increment()
